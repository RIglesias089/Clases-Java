package org.modelo;

public class operadores_Logicos {
    /*Es importante el uso de los operadores logicos en java
    * por el simple hecho que se usan en todos los tipos de lenguaje
    * de progracion, eso incluye C++, python, etc.
    *
    * Hay varios tipos, aritmeticos, logicos y relacionales, asignacion, bits y especiales.
    *
    * En el caso de los aritmeticos tenemos tales como:
    * +,-,*,/,%(modulo o resto de la division), ++ (incremento), --(decremento).
    *
    * En el caso de los Logicos y relacionales tenemos tales como:
    * == (igual a, usado para comprobar o ver si uno es igual a otro)
    * != diferente de (usado apra ver que uno sea diferente a otro)
    * > mayor que, < menor que , >= mayor o igual que, <= menor o igual que
    * && AND logico(Se usa para poder verificar que dos condiciones sean verdaderas al mismo tiempo)
    * || OR logico (Usado para ver que al menos uno en la condicion cumpla que es verdadera)
    * ! NOT logico (Usado para que cambie un booleano, lo niega, si es true pasa a false).
    *
    * En el caso de los de asignacion tenemos tales como:
    * += suma algo a la variable), -= (resta algo a la variable), *= (multiplica algo a la variable
    * /= (divide algo a la variable), %=(guarda el residuo de la division), ++ (incrementa en 1 la variable)
    * -- (decrementa en 1 la variable).
    *
    *
    *
    * */
}
